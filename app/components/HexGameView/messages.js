/*
 * HexGameView Messages
 *
 * This contains all the text for the HexGameView component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.HexGameView';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the HexGameView component!',
  },
});
